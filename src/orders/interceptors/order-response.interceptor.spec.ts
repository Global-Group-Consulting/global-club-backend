import { OrderResponseInterceptor } from './order-response.interceptor';

describe('OrderResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new OrderResponseInterceptor()).toBeDefined();
  });
});
